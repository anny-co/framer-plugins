# Google Sheets Import Plugin

Import items from a Google Sheet into a CMS Collection. Paste a Sheet URL,
map the columns to your Collection fields and import new rows as draft items.
Existing items with matching slugs are skipped.
